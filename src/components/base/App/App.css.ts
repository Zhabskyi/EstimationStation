import { styled } from "@mui/material/styles";
import { ElementType } from "react";

export const Main: ElementType = styled("main")`
  display: flex;
  flex-direction: column;
  background-color: inherit;
`;
